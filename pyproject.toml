[project]
name = "p-086"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "shayneeo", email = "shayneeo@ai20k.build" }
]
requires-python = ">=3.13"
dependencies = [
    "apscheduler>=3.11.3",
    "duckdb>=1.5.5",
    "fastapi>=0.141.1",
    "google-genai>=2.16.0",
    "httpx>=0.28.1",
    "pandas>=3.0.5",
    "pyarrow>=25.0.0",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.14.2",
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "python-dotenv>=1.2.2",
    "python-multipart>=0.0.32",
    "scikit-learn>=1.9.0",
    "uvicorn[standard]>=0.52.1",
]

[project.scripts]
p-086 = "p_086:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
