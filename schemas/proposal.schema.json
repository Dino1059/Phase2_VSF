{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Proposal",
  "description": "v4 agentic proposal — output of agent diagnosis, pending HITL review.",
  "type": "object",
  "properties": {
    "proposal_id": { "type": "string" },
    "session_id": { "type": "string", "description": "FK to agent_traces session — links to the agent investigation that produced this proposal" },
    "snapshot_id": { "type": "string", "description": "FK to raw_snapshots — which data snapshot this proposal is based on" },
    "rules": {
      "type": "array",
      "items": { "$ref": "rulespec.schema.json" },
      "description": "Proposed quality rules (v4 RuleSpec format)"
    },
    "rationale": { "type": "string", "description": "LLM/Agent reasoning explaining WHY these rules are proposed" },
    "status": {
      "type": "string",
      "enum": ["pending", "approved", "rejected", "superseded"],
      "default": "pending"
    },
    "sample_hits": { "type": "integer", "minimum": 0, "description": "Number of rows currently matching this rule — helps human assess impact" },
    "created_at": { "type": "string", "format": "date-time" }
  },
  "required": ["proposal_id", "session_id", "rules", "status"]
}
