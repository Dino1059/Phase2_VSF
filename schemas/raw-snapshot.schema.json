{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RawSnapshot",
  "description": "v4 data lineage snapshot — SHA-256 fingerprint for reproducibility. Aligned with raw_snapshots DDL.",
  "type": "object",
  "properties": {
    "snapshot_id": { "type": "string" },
    "source_name": { "type": "string", "description": "Data source identifier (e.g. google_maps, play_store, vgreen_sensors)" },
    "file_path": { "type": "string", "description": "Path to the raw data file" },
    "sha256_hash": { "type": "string", "pattern": "^[a-f0-9]{64}$", "description": "SHA-256 hash of the data file for immutability verification" },
    "row_count": { "type": "integer", "minimum": 0 },
    "column_count": { "type": "integer", "minimum": 0 },
    "ingested_at": { "type": "string", "format": "date-time" }
  },
  "required": ["snapshot_id", "source_name", "sha256_hash"]
}
