{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CleanDB Onboarding Pack",
  "description": "v4 onboarding pack — generic target schema (data dictionary) for any dataset. Rules now reference v4 RuleSpec with SQL expressions + HITL lifecycle.",
  "type": "object",
  "properties": {
    "schema": {
      "$ref": "#/definitions/CleanDBSchema"
    },
    "rules": {
      "type": "array",
      "items": {
        "$ref": "rulespec.schema.json"
      },
      "description": "Quality rules for this dataset (v4 format — SQL expressions, HITL status)"
    }
  },
  "required": ["schema"],
  "definitions": {
    "CleanDBSchema": {
      "title": "CleanDB Schema",
      "description": "Target Schema & Data Dictionary — single source of truth for dataset column definitions",
      "type": "object",
      "properties": {
        "version": {
          "type": "string"
        },
        "dataset_name": {
          "type": "string"
        },
        "domain": {
          "type": "string",
          "description": "Domain category for cross-domain diagnosis (e.g. feedback, telemetry, bms, trips)"
        },
        "columns": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string"
              },
              "dtype": {
                "type": "string",
                "enum": [
                  "string",
                  "integer",
                  "float",
                  "boolean",
                  "datetime",
                  "text",
                  "json"
                ]
              },
              "nullable": {
                "type": "boolean",
                "default": true
              },
              "description": {
                "type": "string"
              },
              "business_rules": {
                "type": "string"
              },
              "tags": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "unit": {
                "type": "string"
              },
              "valid_range": {
                "type": "object",
                "properties": {
                  "min": {
                    "type": "number"
                  },
                  "max": {
                    "type": "number"
                  }
                }
              },
              "pii": {
                "type": "boolean",
                "default": false
              }
            },
            "required": [
              "name",
              "dtype"
            ]
          }
        }
      },
      "required": [
        "version",
        "dataset_name",
        "columns"
      ]
    }
  }
}
