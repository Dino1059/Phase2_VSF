# Taste

- Wants token/cost-saving tooling (headroom MCP and rtk) actually installed and actively used in the workflow; expects the assistant to verify and report their usage (e.g., shell commands prefixed with rtk per project conventions). Confidence: 0.6
